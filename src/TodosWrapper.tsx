// import React from "react";

// const TodosWrapper = <P extends object>(
//   WrappedComponent: React.ComponentType<P>
// ) => {
//   const MyComponent = (props: P) => {
//     return <WrappedComponent {...props} />;
//   };

//   return <MyComponent />;
// };

// // export default TodosWrapper;

import React from 'react'

const TodosWrapper = () => {
  return (
    <div>TodosWrapper</div>
  )
}

export default TodosWrapper
